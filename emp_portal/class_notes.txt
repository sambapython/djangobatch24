Check in your machine whether python installed or not.
python   command in your command prompt.
if it says python is not an internal or extranal command then you need to install python.

install django
pip install django==3.2.9

type "python" command in command prompt. it will show the python console
import django
django.get_version()

come out from ptyhon prompt
djagno-admin startproject empportal

open empportal folder in any editor.